const envConfigs = {
  exeEnv: {
      host: 'domain',
      authConfig: {
          accessKey: 'Your accessKey.',
          secretKey: 'Your secretKey.',
      }
  }
};

module.exports = envConfigs;

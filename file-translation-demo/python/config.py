server_config = {
    "protocol": "https:",
    "hostname": "translate.rozetta-api.io",
    "port": 443,
}

auth_config = {
    "access_key": "your accessKey",
    "secret_key": "your secretKey",
    "contract_id": "your contractId",
}

config = {
    "server_config": server_config,
    "auth_config": auth_config,
}
